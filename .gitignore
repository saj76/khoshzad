.venv/
__pycache__/
*.pyc
state.json

# contains real coworker names + internal project codenames pulled from the vault — kept local-only
# by default; remove this line if you want it in the repo (e.g. once it's confirmed private)
vault-map.md
